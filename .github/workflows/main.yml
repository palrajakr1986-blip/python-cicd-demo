name: Python CI Pipeline

# Trigger workflow on pushes to master
on:
  push:
    branches: [ master ]

jobs:
  run-python:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.13'

      - name: Run Python program
        run: python add.py
